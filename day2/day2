# Week 6 / Day 2 — Quick Sort (Recursion)

## README.md

### Title

**Quick Sort — Recursion**
``cpp
#include<iostream>
#include<vector>
using namespace std;
int partition(vector<int> &arr,int st,int end){
    int pivotEle=arr[end],idx=st-1;
    for(int i=st;i<end;i++){
        if(arr[i]<=pivotEle){
             idx++;
            swap(arr[i],arr[idx]);
        }
    }
    idx++;
    swap(arr[end],arr[idx]);
    return idx;
}
void quickSort(vector<int> &arr,int st,int end){
    if(st<end){
       int pivot = partition(arr,st,end);
        quickSort(arr,st,pivot-1);
        quickSort(arr,pivot+1,end);
    }
}
int main(){
    vector<int> arr={12,31,8,9,65,45};
    quickSort(arr,0,arr.size()-1);
    for(int val : arr){
        cout << val <<" ";
    }
    cout<<endl;
    return 0;
}
